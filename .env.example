# Copy this file to .env.local for local development if you want Google Maps to work locally.
# In production (Render), set this as an Environment Variable in the Render dashboard instead —
# do not commit a real key to this file or to GitHub.

VITE_GOOGLE_MAPS_KEY=
