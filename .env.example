# Copy this file to .env.local for local development.
# In production (Render), set these as Environment Variables in the Render dashboard instead —
# do not commit real values to this file or to GitHub.

VITE_GOOGLE_MAPS_KEY=
VITE_API_URL=http://localhost:3001/api
